apiVersion: apps/v1
kind: Deployment
metadata:
  name: home-web
spec:
  template:
    spec:
      containers:
      - name: nginx
        resources:
          requests:
            memory: "12Mi"
            cpu: "25m"
          limits:
            memory: "48Mi"
            cpu: "100m"
