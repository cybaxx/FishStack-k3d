apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: forum
  namespace: flux-system
spec:
  interval: 5m
  path: ./services/forum/k8s/overlays/staging
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  healthChecks:
    - apiVersion: apps/v1
      kind: Deployment
      name: forum-web
      namespace: wetfish-staging
    - apiVersion: apps/v1
      kind: Deployment
      name: forum-mysql
      namespace: wetfish-staging
  timeout: 5m
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  dependsOn:
    - name: infrastructure
